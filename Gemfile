source 'https://rubygems.org'

gem 'rack' # Strictly speaking 1.5.0 is the one that introduced the Hijack API

group :development do
  gem 'rspec', '~> 3'
  gem 'puma'
  platform :mri, :rbx do
    gem 'sendfile'
  end
end
